# AGENTS.md

<!--
生成与维护要求：
- 仓库源代码中，本模板维护于 `skills/engineering/setup-skills/assets/`，修改时以此为准。
- 只写已经从仓库或用户确认的信息，不推测项目事实。
- 按项目实际情况保留 sections；删除空 section、comments 与 placeholders。
- 以 manifests、scripts、config 和目录结构作为可直接查询事实的 source of truth；这里只记录会改变 agent 决策的约定、边界、原因和非显而易见的入口。
- 项目结构、命令或约束变化时同步维护生成后的 `AGENTS.md`。
-->

## 项目上下文

<!--
- 说明项目目标、仓库边界，以及它在更大系统中的职责。
- 仅记录会影响实现选择且无法从 manifests 直接确认的技术栈约束。
-->

## 架构与代码归属

<!--
- 说明模块职责、依赖方向、架构边界，以及新代码应放置的位置。
- 仅列出职责不直观或有特殊约束的路径，不复制完整目录树。
- 记录违反边界时应采用的替代 seam 或既有扩展点。
-->

## 开发环境与运行

<!--
- 记录必要前置条件，以及 canonical setup、build、start 入口；优先引用仓库已有脚本。
- 对 DB、Redis、MQ 等外部服务，只记录非敏感依赖拓扑、本地替代方案、环境变量名称和凭据获取方式，不写入密码、token 或连接密钥。
- 仓库已有 Docker 或 Compose 支持时，说明 canonical 入口和适用场景；缺少容器配置本身不授权新增基础设施。
-->

## 验证

<!--
- 按仓库实际层次记录前端、后端和集成测试入口。
- 说明不同变更类型对应的最低验证集合、必要执行顺序和可检查的完成条件。
- 某项验证无法运行时，要求报告未运行项、原因和剩余风险。
-->

## 变更约束

<!--
- 记录 formatter、linter 或 Checkstyle 之外仍需人工说明的代码约定。
- 记录数据库 migration、SQL 脚本、生成文件和提交工作流的项目特有约束。
- 规则冲突或存在多个会实质改变结果的方案时，列出选择并请求用户决定。
-->

## 安全与人工确认

<!--
- 说明 secrets 的本地管理方式和团队认可的凭据获取流程；任何敏感值都不写入仓库文档。
- 标明会修改生产环境、外部系统、共享数据或造成不可恢复结果的操作；执行前确认准确目标并获得用户明确授权。
- 对普通本地验证入口与需要额外授权的脚本作出可执行区分，避免笼统的 shell 禁令。
-->

## Context pointers

<!--
- 每条 pointer 同时说明文档内容和读取它的任务分支。
- 示例：修改领域术语、业务规则或跨模块行为前，读取 `CONTEXT.md`。
- 示例：修改受架构决策约束的模块前，读取相关 `docs/adr/`；目标不存在时不生成该 pointer。
-->

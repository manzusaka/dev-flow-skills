// Generated by scripts/embed-schemas.mjs from skills/engineering/init-cli/assets/schemas.
// Do not edit by hand — edit the assets and re-run `npm run build`.

export const EMBEDDED_CONFIG_YAML = "schema: spec-driven\n\ncontext: |\n  Use CONTEXT.md as the source of truth for domain language.\n  Read relevant docs/adr/ records before changing architecture-constrained modules.\n";

export const EMBEDDED_SCHEMA_YAML = "name: spec-driven\nversion: 1\ndescription: 默认 OpenSpec 工作流 - proposal → specs → design → tasks\nartifacts:\n  - id: proposal\n    generates: proposal.md\n    description: 阐述变更缘由的初始 proposal 文档\n    template: proposal.md\n    instruction: |\n      创建 proposal 文档，说明为什么（WHY）需要这次变更。\n\n      章节：\n      - **Why**：用 1-2 句话说明问题或机会。这解决了什么问题？为什么是现在？\n      - **User story**：按 `US-NNN` 编号记录已经确定的业务场景。每条包含变更类型、场景/约束、图片/原型、图片说明与故事级验收标准（正常功能与边界的简单描述，不写形式化测试用例——它们由 spec 的 Scenario 拆解）；没有图片或原型时明确写“无”。\n      - **Capabilities**：指出哪些 specs 将被创建或修改：\n        - **New Capabilities**：列出引入的新能力。每一项对应一个新的 `specs/<capability-path>/spec.md`。你新引入的路径片段使用 kebab-case（例如 `user-auth` 或 `identity/user-auth`），并遵循项目现有的 spec 组织方式。\n        - **Modified Capabilities**：列出需求（REQUIREMENTS）发生变化的既有能力。仅当 spec 层面的行为发生变化时才列出（只改实现细节不算）。每一项都需要一个增量规范（delta spec）文件。使用 `openspec/specs/` 下已存在的精确路径。若没有需求变化则留空。\n      - **Impact**：受影响的代码、API、依赖或系统。\n\n      重要：Capabilities 章节至关重要。它构成 proposal 阶段与 specs 阶段之间的契约。\n      填写前请先调研既有的 specs。此处列出的每个能力都需要一个对应的 spec 文件。\n\n      每个变更要么至少声明一个能力（新增或修改），要么显式声明不需要 specs：\n      `spect validate` 会拒绝零 delta 的变更，除非该变更的 `.openspec.yaml`\n      设置了 `skip_specs: true`。仅当确实没有 spec 层面的行为变化时（纯重构、\n      工具链、文档）才使用 `skip_specs: true` —— specs 描述的是行为，\n      所以如果行为没有变化，也就不应该有 spec 变化。\n      不要为了通过校验而臆造需求。\n\n      保持简洁（1-2 页）。聚焦于\"为什么\"而非\"怎么做\" ——\n      实现细节应写在 design.md 中。\n\n      这是整个流程的基础 —— specs、design 和 tasks 都建立在它之上。\n    requires: []\n\n  - id: specs\n    generates: \"specs/**/*.md\"\n    description: 本次变更的详细规范\n    template: spec.md\n    instruction: |\n      创建规范文件，定义系统应该做什么（WHAT）。\n\n      spec 是行为契约，不是实现计划。\n\n      好的 spec 内容包含：\n      - 用户或下游系统所依赖的可观测行为\n      - 输入、输出与错误条件\n      - 外部约束（安全性、隐私、可靠性、兼容性）\n      - 可被测试或显式验证的场景\n\n      spec 中应避免：\n      - 内部类名/函数名\n      - 库或框架的选型\n      - 逐步的实现细节\n      - 详细的执行计划（这些属于 design.md 或 tasks.md）\n\n      快速判断法：如果实现可以变化而不改变外部可见行为，那它很可能不属于 spec。\n\n      为 proposal 的 Capabilities 章节中列出的每个能力创建一个 spec 文件。\n      `<capability-path>` 是相对于 `specs/` 的 spec 目录（例如\n      `user-auth` 或 `identity/user-auth`）。请保留完整路径：\n      - 新能力：使用 proposal 中给出的精确路径，创建于 `specs/<capability-path>/spec.md`。proposal 中新引入的任何路径片段都必须是 kebab-case。遵循项目现有的组织方式；当项目采用扁平布局时，不要新增一层领域目录。\n      - 修改的能力：在 `specs/<capability-path>/spec.md` 创建 delta 时，使用 `openspec/specs/<capability-path>/` 下已存在的精确路径。不要移动或重命名该能力。\n\n      必须至少有一个 spec 文件，除非该变更的 `.openspec.yaml`\n      设置了 `skip_specs: true`（表示没有 spec 层面的行为变化）——\n      `spect validate` 会拒绝没有该标记的零 delta 变更。\n      如果 proposal 未列出任何能力且未设置 `skip_specs`，请先回头修订 proposal。\n\n      Delta 操作（使用 ## 标题）：\n      - **ADDED Requirements**：新增能力\n      - **MODIFIED Requirements**：变更的行为 —— 必须（MUST）包含完整的更新后内容\n      - **REMOVED Requirements**：废弃的特性 —— 必须（MUST）包含 **Reason** 与 **Migration**\n      - **RENAMED Requirements**：仅名称变更 —— 使用 FROM:/TO: 格式\n\n      格式要求：\n      - 每条需求：`### Requirement: <名称>`，其后接描述\n      - 规范性需求使用 SHALL/MUST（避免 should/may）\n      - 每个场景：`#### Scenario: <名称>`，采用 WHEN/DO/THEN 格式：`- **WHEN**` 为前置条件与测试数据，`- **DO**` 为操作步骤（多步用 → 串联成一行），`- **THEN**` 为预期结果，`- **AND**` 可选补充断言\n      - 一条 Scenario 场景名称：可独立验证的用例；分支请拆分为多条 Scenario，并在场景名中写明分支条件\n      - Scenario 是 proposal User story 中验收标准的形式化：验收标准在故事层面描述正常功能，Scenario 将其拆解为可逐条验证的测试用例\n      - **关键**：场景必须（MUST）正好使用 4 个井号（`####`）。使用 3 个井号或列表项会静默失败。\n      - 每条需求必须（MUST）至少包含一个场景。\n\n      仅限新能力：增量规范以 `## Purpose` 章节开头 ——\n      用一到两句话（50 个字符以上，否则 `spect validate --strict`\n      会报告其过于简略）描述该能力的用途。归档时会将其\n      复制到所创建的主 spec 中；缺少它时，新的主 spec 会留下\n      `TBD ... Update Purpose after archive` 占位符，需要手工填写。\n      不要为既有能力的 delta 添加 `## Purpose` ——\n      该 spec 已经有一个，delta 中的会被忽略。要修改\n      既有能力的 Purpose（包括残留的 `TBD` 占位符），\n      请直接编辑 `openspec/specs/<capability-path>/spec.md`。\n\n      MODIFIED 需求的工作流程：\n      1. 在 openspec/specs/<capability-path>/spec.md 中定位既有需求\n      2. 复制整个需求块（从 `### Requirement:` 到其所有场景）\n      3. 粘贴到 `## MODIFIED Requirements` 下，并编辑以反映新行为\n      4. 确保标题文本完全一致（忽略空白差异）\n\n      常见陷阱：MODIFIED 只写部分内容会在归档时丢失细节。\n      如果是在不改变既有行为的前提下新增关注点，请改用 ADDED。\n\n      示例（一个新能力，因此以 `## Purpose` 开头）：\n      ```\n      ## Purpose\n\n      让用户以可移植的格式导出自己在产品中的数据。\n\n      ## ADDED Requirements\n\n      ### Requirement: User can export data\n      The system SHALL allow users to export their data in CSV format.\n\n      #### Scenario: Successful export\n      - **WHEN** user clicks \"Export\" button\n      - **THEN** system downloads a CSV file with all user data\n\n      ## REMOVED Requirements\n\n      ### Requirement: Legacy export\n      **Reason**: Replaced by new export system\n      **Migration**: Use new export endpoint at /api/v2/export\n      ```\n\n      specs 应当是可测试的 —— 每个场景都是一个潜在的测试用例。\n    requires:\n      - proposal\n\n  - id: design\n    generates: design.md\n    description: 包含实现细节的技术设计文档\n    template: design.md\n    instruction: |\n      创建 design 文档，说明如何（HOW）实现这次变更。\n\n      何时需要 design.md（满足任一条件才创建）：\n      - 横切变更（涉及多个服务/模块）或引入新的架构模式\n      - 新增外部依赖或数据模型有重大变化\n      - 涉及安全、性能或迁移复杂度\n      - 存在歧义，需要在编码前先做技术决策\n\n      章节：\n      - **Context**：仅写解释该方案所必需的现状与约束。动机部分引用 proposal 而不要复述（例如\"参见 proposal.md - Why\"）。\n      - **Goals / Non-Goals**：本设计要达成什么、明确排除什么。不要复述 proposal 的范围 —— 只补充设计层面的边界。\n      - **Decisions**：关键技术选型及其理由（为什么选 X 而不是 Y？）。为每个决策附上考虑过的备选方案。已确认的 test seams（测试接入系统的入口与钩子）也记录在这里。\n      - **Risks / Trade-offs**：已知的局限、可能出问题的地方。格式：[风险] → 缓解措施\n      - **Migration Plan**：部署步骤、回滚策略（如适用）\n      - **Open Questions**：可以安全地留待以后回答、且不会改变 specs、\n        方案或任务拆分的未知项。若没有则省略。\n\n      Open Questions 用于真正可延后的未知项，而不是你跳过的决策。\n      如果某个问题会改变 specs、既定方案或任务拆分，\n      现在就要解决它 —— 去询问用户，而不是靠猜。\n\n      聚焦于架构与方案，而非逐行实现。\n      proposal 讲清为什么和做什么；design 讲清怎么做。动机引用\n      proposal，specs 写好后需求引用 specs ——\n      如果某个章节只是复述它们，那就直接指向它们。\n\n      好的设计文档会解释技术决策背后的\"为什么\"。\n    requires:\n      - proposal\n\n  - id: tasks\n    generates: tasks.md\n    description: 可追踪进度的实现清单\n    template: tasks.md\n    instruction: |\n      创建任务列表，拆解实现工作。\n\n      编写任务前，检查 design.md 中的 Open Questions。如果其中任何一项\n      会改变要构建的内容，先与用户一起解决 —— 不要\n      把未言明的假设固化进任务列表。\n\n      **重要：严格遵循下面的模板。** apply 阶段通过解析\n      复选框格式来追踪进度。不使用 `- [ ]` 的任务不会被追踪。\n\n      指引：\n      - 将相关任务归入 ## 编号标题下\n      - 每个任务必须（MUST）是复选框：`- [ ] X.Y 任务描述`\n      - 任务应足够小，可在一次会话内完成\n      - 按依赖关系排序（什么必须先做？）\n\n      示例：\n      ```\n      ## 1. 准备工作\n\n      - [ ] 1.1 创建新的模块结构\n      - [ ] 1.2 在 package.json 中添加依赖\n\n      ## 2. 核心实现\n\n      - [ ] 2.1 实现数据导出函数\n      - [ ] 2.2 添加 CSV 格式化工具\n      ```\n\n      需要构建什么参考 specs，怎么构建参考 design。\n      每个任务都应当是可验证的 —— 你能判断它何时完成。\n    requires:\n      - specs\n      - design\n\napply:\n  requires: [tasks]\n  tracks: tasks.md\n  instruction: |\n    阅读上下文文件，逐项完成待办任务，边做边标记完成。\n    遇到阻塞或需要澄清时暂停。\n";

export const EMBEDDED_TEMPLATES: Record<string, string> = {
  proposal: "## Why\n\n<!-- 说明这次变更的动机。它解决了什么问题？为什么是现在？ -->\n\n## User story\n\n<!-- 已标准用户故事的方式，描述出变更的内容。明确写出新增能力、修改内容或移除项。 -->\n\n每个故事的格式如下：\n```\nUS-001：具体业务场景下、用户角色需要实现的业务功能描述。\n类型：功能新增/修改/删除\n场景/约束：：前置业务流程、条件、约束等\n图片/原型：\n图片说明：原型补充说明。\n验收标准：故事级正常功能与边界的简单描述；形式化测试用例由 spec 的 Scenario 拆解。\n```\n\n## Capabilities\n\n### New Capabilities\n<!-- 引入的新能力。你新引入的路径片段使用 kebab-case\n     （例如 user-auth 或 identity/user-auth），并遵循项目现有的\n     spec 组织方式。每一项都会创建 specs/<capability-path>/spec.md。 -->\n- `<capability-path>`: <这个能力涵盖内容的简要描述>\n\n### Modified Capabilities\n<!-- 需求（REQUIREMENTS）发生变化的既有能力（只改实现不算）。\n     仅当 spec 层面的行为发生变化时才列在这里。每一项都需要一个增量规范文件。\n     使用 openspec/specs/ 下已存在的精确路径。若没有需求变化则留空。\n     完全没有涉及任何能力的变更（纯重构、工具链、文档）\n     必须在其 .openspec.yaml 中设置 `skip_specs: true` ——\n     spect validate 会拒绝没有该标记的零 delta 变更。\n     不要为了通过校验而臆造需求。 -->\n- `<existing-capability-path>`: <哪条需求正在发生变化>\n\n## Impact\n\n<!-- 受影响的代码、API、依赖、系统 -->\n",
  spec: "## Purpose\n<!-- 引用@proposal.md中## User story，清晰描述 spec要实现的功能用途。 -->\n\n## ADDED Requirements\n\n### Requirement: <!-- 需求名称 -->\n<!-- 通过User story拆解出的二级需求名称 -->\n\n#### Scenario: <!-- 场景名称：可独立验证的用例；分支请拆分为多条 Scenario -->\n- **WHEN** <!-- 前置条件与测试数据 -->\n- **DO** <!-- 操作步骤；多步用 → 串联成一行 -->\n- **THEN** <!-- 预期结果 -->\n- **AND** <!-- 补充断言，可选 -->\n",
  design: "## Context\n\n<!-- 影响方案选择的现状与约束。动机参见 proposal.md，不要在此复述 -->\n\n## Goals / Non-Goals\n\n**Goals:**\n<!-- 本设计要达成的目标 -->\n\n**Non-Goals:**\n<!-- 明确排除在范围之外的内容 -->\n\n## Decisions\n\n<!-- 关键设计决策，附理由与考虑过的备选方案 -->\n\n## Risks / Trade-offs\n\n<!-- 已知的风险与权衡 -->\n",
  tasks: "## 1. <!-- 任务组名称 -->\n\n- [ ] 1.1 <!-- 任务描述 -->\n- [ ] 1.2 <!-- 任务描述 -->\n\n## 2. <!-- 任务组名称 -->\n\n- [ ] 2.1 <!-- 任务描述 -->\n- [ ] 2.2 <!-- 任务描述 -->\n",
};
